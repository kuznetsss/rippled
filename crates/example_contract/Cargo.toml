[package]
name = "example_contract"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
stdlib = { path = "../stdlib" }

[profile.release]
panic = "abort"
opt-level = "s"
lto = true
